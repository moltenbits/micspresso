# Local signing config for scripts/bundle.sh (copy to .env, which is gitignored).
# When both are set, bundles are signed with your Developer ID Application
# identity; otherwise they're ad-hoc signed for local use.
TEAM_NAME="Your Team Name"
TEAM_ID="ABCDE12345"
